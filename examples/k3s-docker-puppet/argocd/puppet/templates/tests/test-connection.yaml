apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "puppet-agent.fullname" . }}-test-connection"
  labels:
{{ include "puppet-agent.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "puppet-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
